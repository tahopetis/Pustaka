---
phase: 02-entity-management
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/views/ea/EntityListView.vue
autonomous: true
requirements:
  - ENT-01
  - ENT-02
gap_closure: true

must_haves:
  truths:
    - "AG Grid components render without module registration errors"
    - "Entity list view displays EA entities in data grid"
    - "AG Grid features work (sorting, filtering, pagination)"
  artifacts:
    - path: "web/src/views/ea/EntityListView.vue"
      provides: "AG Grid module registration and entity list rendering"
      min_lines: 200
      contains: "ModuleRegistry.registerModules([AllCommunityModule])"
  key_links:
    - from: "EntityListView.vue"
      to: "ag-grid-community"
      via: "ModuleRegistry.registerModules() call"
      pattern: "ModuleRegistry\\.registerModules\\(\\[AllCommunityModule\\]\\)"

---

<objective>
Fix AG Grid module registration error in EntityListView to enable entity list rendering

Purpose: The frontend EntityListView imports AG Grid components but never registers the required AG Grid modules, causing error #272 and preventing entity display. AG Grid v31+ requires explicit module registration before component usage.
Output: Working entity list view with functional AG Grid table
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-entity-management/02-UAT.md
@.planning/phases/02-entity-management/02-02-SUMMARY.md
@web/src/views/ea/EntityListView.vue
</context>

<tasks>

<task type="auto">
  <name>Register AG Grid Community Modules</name>
  <files>web/src/views/ea/EntityListView.vue</files>
  <action>
    In web/src/views/ea/EntityListView.vue:

    1. Add to imports section (around line 160-168):
       ```javascript
       import { ModuleRegistry, AllCommunityModule } from 'ag-grid-community'
       ```

    2. Add module registration BEFORE the component definition (after imports, before defineComponent):
       ```javascript
       // Register AG Grid modules (required for v31+)
       ModuleRegistry.registerModules([AllCommunityModule])
       ```

    This ensures AG Grid modules are available when the component renders. AllCommunityModule includes all core AG Grid features (sorting, filtering, pagination).

    Reference existing import structure - AG Grid is already imported but modules not registered.
  </action>
  <verify>
    grep -n "ModuleRegistry.registerModules" web/src/views/ea/EntityListView.vue
    grep -n "AllCommunityModule" web/src/views/ea/EntityListView.vue
  </verify>
  <done>
    AG Grid module registration present in EntityListView.vue with AllCommunityModule imported and registered before component export
  </done>
</task>

<task type="auto">
  <name>Rebuild Frontend Docker Container</name>
  <files>docker-compose.yml</files>
  <action>
    Rebuild the frontend container to apply AG Grid module registration fix:

    ```bash
    docker compose down frontend
    docker compose up -d --build frontend
    ```

    Wait for container to be healthy (check logs for "Vite server started" message).
  </action>
  <verify>
    docker compose ps frontend | grep -q "Up"
    docker compose logs frontend | tail -20
  </verify>
  <done>
    Frontend container rebuilt and running without errors
  </done>
</task>

<task type="auto">
  <name>Verify AG Grid Error Resolution</name>
  <files>web/src/views/ea/EntityListView.vue</files>
  <action>
    Check that AG Grid module error is resolved by examining browser console logs after container rebuild:

    1. Visit http://localhost:3000/entities/business (or any EA entity list)
    2. Open browser DevTools Console
    3. Verify no "AG Grid error #272" or "No AG Grid modules registered" messages
    4. Check that AG Grid initializes successfully

    The fix should eliminate the module registration error that was blocking entity display.
  </action>
  <verify>
    docker compose logs frontend | grep -i "ag-grid\|module\|error" | tail -20
  </verify>
  <done>
    AG Grid loads without module registration errors in browser console
  </done>
</task>

</tasks>

<verification>
- [ ] ModuleRegistry.registerModules([AllCommunityModule]) present in EntityListView.vue
- [ ] AllCommunityModule imported from 'ag-grid-community'
- [ ] Frontend container rebuilt successfully
- [ ] No AG Grid module registration errors in browser console
- [ ] AG Grid table renders on entity list page
</verification>

<success_criteria>
AG Grid module registration error resolved - Entity list view can render AG Grid table without "No AG Grid modules registered" error
</success_criteria>

<output>
After completion, create `.planning/phases/02-entity-management/02-05-SUMMARY.md`
</output>
