---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/migrations/009_add_ea_metamodel.up.sql
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - INT-01
  - INT-05
gap_closure: true

must_haves:
  truths:
    - "Migration 009 applies successfully without errors"
    - "All 60 EA CI types created across all 8 domains"
    - "All 23 EA relationship types created with correct column names"
    - "Migration is idempotent (can run multiple times safely)"
  artifacts:
    - path: "cmd/migrations/009_add_ea_metamodel.up.sql"
      provides: "Fixed EA metamodel migration"
      contains: "Corrected JSON syntax and column names"
  key_links:
    - from: "cmd/migrations/009_add_ea_metamodel.up.sql"
      to: "cmd/migrations/003_fix_relationship_types_schema.sql"
      via: "Column name alignment"
      pattern: "reverse_label|allowed_source_types|allowed_target_types"
---

<objective>
Fix EA metamodel migration (009) to successfully create all 60 EA CI types and 23 EA relationship types by correcting JSON syntax errors and aligning column names with post-migration 003 schema.

Purpose: Close gaps identified in UAT testing where migration partially failed due to documentation-driven development using outdated schema references
Output: Working migration that creates all EA artifacts without errors
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@cmd/migrations/003_fix_relationship_types_schema.sql
@cmd/migrations/009_add_ea_metamodel.up.sql
</context>

<tasks>

<task type="auto">
  <name>Fix JSON syntax errors in CI type INSERT statements</name>
  <files>cmd/migrations/009_add_ea_metamodel.up.sql</files>
  <action>
    Fix 5 JSON syntax errors in optional_attributes columns that are causing CI type INSERTs to fail:

    Line 54 (EA.Strategy-Outcome):
    BEFORE: '[{"name":"measurement_criteria","type":"string"},{"name="target_date","type":"date"}]'
    AFTER:  '[{"name":"measurement_criteria","type":"string"},{"name":"target_date","type":"date"}]'

    Line 57 (EA.Strategy-Initiative):
    BEFORE: '[{"name":"status",...},{"name":"budget","type":"string"},{"name="start_date","type":"date"},{"name="end_date","type":"date"}]'
    AFTER:  '[{"name":"status",...},{"name":"budget","type":"string"},{"name":"start_date","type":"date"},{"name":"end_date","type":"date"}]'

    Line 82 (EA.Application-Function):
    BEFORE: '[{"name":"parent_component_id","type":"uuid"},{"name="complexity","type":"string","enum":["low","medium","high"]}]'
    AFTER:  '[{"name":"parent_component_id","type":"uuid"},{"name":"complexity","type":"string","enum":["low","medium","high"]}]'

    Line 132 (EA.Security-Vulnerability):
    BEFORE: '[{"name":"severity","type":"string","enum":["critical","high","medium","low"}]}]'
    AFTER:  '[{"name":"severity","type":"string","enum":["critical","high","medium","low"]}]'

    Root cause: Missing closing quotes on property names (name= instead of name":"), malformed enum arrays (} instead of ])
  </action>
  <verify>grep -n 'name="' cmd/migrations/009_add_ea_metamodel.up.sql returns no results</verify>
  <done>All CI type INSERT statements have valid JSON syntax</done>
</task>

<task type="auto">
  <name>Fix column names in relationship type INSERT statements</name>
  <files>cmd/migrations/009_add_ea_metamodel.up.sql</files>
  <action>
    Update line 153 INSERT column list to use correct column names from migration 003 schema:

    BEFORE:
    INSERT INTO relationship_types (name, description, forward_label, backward_label, source_types, target_types, ...)

    AFTER:
    INSERT INTO relationship_types (name, description, forward_label, reverse_label, allowed_source_types, allowed_target_types, ...)

    Column mappings from migration 003:
    - backward_label → reverse_label
    - source_types → allowed_source_types
    - target_types → allowed_target_types

    This affects the initial INSERT statement at line 153 and all 23 relationship type value rows.

    Reference from 01-02-SUMMARY.md: "Migration 009 created from planning documents that referenced migration 002 schema. Migration 003 renamed backward_label→reverse_label and source_types→allowed_source_types but planning docs were not updated."
  </action>
  <verify>grep -n "backward_label\|source_types\|target_types" cmd/migrations/009_add_ea_metamodel.up.sql | grep -v "allowed_" returns no results</verify>
  <done>Relationship type INSERTs use correct column names matching migration 003 schema</done>
</task>

<task type="auto">
  <name>Verify migration syntax and test idempotency</name>
  <files>cmd/migrations/009_add_ea_metamodel.up.sql</files>
  <action>
    1. Validate JSON syntax in all optional_attributes and required_attributes columns using psql's jsonb validation
    2. Confirm all INSERT statements have ON CONFLICT (name) DO NOTHING for idempotency
    3. Verify column count matches between INSERT column list and all value tuples
    4. Check that all JSONB casts (::jsonb) are properly formatted
    5. Ensure all subqueries (SELECT id FROM users WHERE username = 'admin') are valid

    Test command: psql -h localhost -U pustaka -d pustaka -c "\set ON_ERROR_STOP 1" -f cmd/migrations/009_add_ea_metamodel.up.sql

    Expected result: Migration completes with no errors, creates 60 EA CI types and 23 EA relationship types
  </action>
  <verify>
    docker exec -it pustaka-postgres psql -U pustaka -d pustaka -c "SELECT COUNT(*) FROM ci_type_definitions WHERE name LIKE 'EA.%';" returns 60
    docker exec -it pustaka-postgres psql -U pustaka -d pustaka -c "SELECT COUNT(*) FROM relationship_types WHERE name IN ('supports', 'depends_on', 'realizes', 'flows_to', 'assigned_to', 'aggregates', 'composes', 'accesses', 'associated_with', 'deployed_on', 'runs_on', 'uses', 'implements', 'validates', 'mitigates', 'enforces', 'assesses', 'governs', 'aligned_with', 'conforms_to', 'derived_from', 'decomposes', 'triggers');" returns 23
  </verify>
  <done>Migration applies successfully creating all EA artifacts, can be rerun without errors due to ON CONFLICT clauses</done>
</task>

</tasks>

<verification>
After completing all tasks, verify that:
1. Migration file has no JSON syntax errors (all property names properly quoted)
2. Migration file uses correct column names (reverse_label, allowed_source_types, allowed_target_types)
3. Migration executes successfully against database
4. All 60 EA CI types exist in ci_type_definitions table
5. All 23 EA relationship types exist in relationship_types table
6. Migration is idempotent (running twice produces no errors)
</verification>

<success_criteria>
- Migration 009 applies without errors
- Database contains exactly 60 EA CI types (8 teams ✅, 60 CI types ✅, 23 relationship types ✅, 4 permissions ✅)
- All 8 domains represented: Strategy (6), Business (10), Application (8), Data (7), Technology (8), Infrastructure (8), Security (6), Governance (7)
- All relationship types include both ArchiMate core and EA-specific types
- Migration can be safely rolled back using 009 down.sql
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
